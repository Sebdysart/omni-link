# Examples

These sample layouts show how to point omni-link at different ecosystem shapes.

- `basic-setup/` is the smallest backend + frontend pairing.
- `microservices/` shows a multi-service backend graph.
- `mobile-backend/` shows a native client paired with an API service.
